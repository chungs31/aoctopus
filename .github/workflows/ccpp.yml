name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v2
    - name: getdeps
      run: source /home/chungs31/tvm-dev-19.1.sh
    - name: make
      run: make
